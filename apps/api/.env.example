# Copy this file to .env and adjust values as needed.
# Default uses SQLite so no external database is required (ideal for Codespaces).

DATABASE_URL=sqlite:///./crm.db
SECRET_KEY=change-me-in-production
